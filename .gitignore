# Dependencies
node_modules/

# Playwright output
test-results/
playwright-report/

# Auth state (created at runtime by global.setup.ts)
test-data/.auth/

# Local config — contains your API key and absolute path; never commit this
.claude/settings.local.json

# Local working files (personal, not committed)
my-test.txt
prd.md
prd-tests.txt
coverage-report.md
coverage-gaps.txt

# macOS
.DS_Store
.setup-guide.md
