{
  "//": "Optional. Copy to settings.local.json to pre-approve common Claude Code commands for this project so you are not prompted on every run. The file is gitignored — safe to customise per machine.",
  "permissions": {
    "allow": [
      "Bash(npx tsc *)",
      "Bash(npm install *)",
      "Bash(npx playwright *)"
    ]
  }
}
