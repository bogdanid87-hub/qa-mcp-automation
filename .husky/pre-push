#!/bin/sh
# Run the full Playwright suite before pushing — tests hit the live site
# from your local machine (trusted IP) rather than from CI cloud runners.
# Use git push --no-verify to skip if you need an emergency push.
echo "⏳ Running Playwright tests before push (npm test)..."
npm test

