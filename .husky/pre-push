#!/bin/sh
# Skip in CI — GitHub Actions has its own quality gates (tsc + MCP schema validation).
# The hook is for local pushes only, where tests run from a trusted IP address.
[ "${CI}" = "true" ] && exit 0

echo "⏳ Running Playwright tests before push (npm test)..."
npm test
